# Implementation Plan

1. Environment, collaboration evidence, independent proposals, debate, ownership.
2. Methodology contract, policy schema, independent tests and golden commitments.
3. Pure credit engine, scorecard, capacity, stress, reverse stress, covenants, decision, memo provenance.
4. PostgreSQL schema, migrations, FastAPI contracts, read-only mode, exports and audit.
5. Image-generated visual concept, design tokens, Next.js shell, guided wizard and case pages.
6. Three golden cases, generated API demo fixtures, 40-borrower synthetic portfolio and Power BI specification.
7. Twenty-tab Excel reference model and reconciliation.
8. Unit/integration/component/E2E/accessibility/security/performance/visual verification; usability simulations and fixes.
9. README, architecture diagram, screenshots, demo script, reports, resume bullets, deployment attempt and honest gate table.

No phase is accepted without non-author diff review and a green local `verify` entry point.

The full Excel workbook is generated later than the master prompt's phase ordering, but its formula specification and golden hashes precede the engine. This preserves independent calculation verification while avoiding early layout work that does not unblock engine correctness; DEC-008 records the deviation.
