# Historical Model Configuration Pointer

This historical path is retained so earlier collaboration records do not break.

The canonical current configuration is:

`docs/collaboration/model-config.md`

All new collaboration records must cite that file and the real Claude Code session ID.
